apiVersion: v1
kind: Service

metadata:
  name: travel-booking-service

spec:

  type: NodePort

  selector:
    app: travel-booking-app

  ports:

  - port: 8082
    targetPort: 8081
    nodePort: 30082

